namespace Gym_Booking_Manager
{
    // Just a test.....so this may not last!! 
    public interface IReservingEntity
    {
        //Perhapes a bool, to distinguish  if ResrvingEntity is eligible for reservations
        bool AbleToMakeReservation();

        //What should this Interface do?
        //Only the brave will find out!
    }
}